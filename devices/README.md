# Devices

Device directories contain hardware-specific engineering dossiers.

Each device should keep concrete fixes, evidence boundaries, package/source baselines, and recovery notes separate from reusable knowledge in `learnings/`, `patterns/`, and `standards/`.
